{% extends 'security/base.html.twig' %}

{% block title %}Admin Connexion{% endblock %}

{% block body %}
<section class="login-admin">
    <div class="container">
        {% for message in app.flashes('success') %}
                <div class="alert alert-success">
                    {{message}}
                </div>
            {% endfor %}
        <div class="row">
            <div class="col-xl-8 col-lg-8 col-md-12 col-sm-12">
                <h1 class="fw-bold mb-4 admin-title">Test Technique E-Talik</h1>
                <p class="login-para">
                    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed accumsan, 
                    quam in tristique malesuada, ex erat venenatis lectus, nec facilisis elit tortor vel sapien. 
                    Nulla facilisi. Sed ac elit eu ligula euismod consectetur eu vel justo. 
                    Proin a nisl a risus vestibulum ultricies. Integer aliquam felis eget enim finibus, 
                    eu ultrices urna hendrerit. Mauris vel ligula vitae leo semper auctor. 
                </p>
            </div>
            <div class="col-xl-4 col-lg-4 col-md-12 col-sm-12">
                <div class="form-login shadow p-4">
                    <h4 class="text-center text-white mb-4">Connectez-vous</h4>
                    {{form_start(form)}}
                    {% if error %}
                        <div class="alert alert-danger">Le nom d'utilisateur ou le mot de passe n'est pas valide</div>
                    {% endif %}
                    
                    <div class="input-group mb-4">
                      <div class="input-group-prepend">
                          <span class="input-group-text" style="border-right: none; border-radius: 0.2rem 0 0 0.2rem; background-color: #fff;">
                              <i class="fas fa-envelope" style="height: 24px; padding-top: 5px;"></i>
                          </span>
                      </div>
                  {{form_widget(form.email)}}
                  </div>

                  <div class="input-group mb-4">
                      <div class="input-group-prepend">
                          <span class="input-group-text" style="border-right: none; border-radius: 0.2rem 0 0 0.2rem; background-color: #fff;">
                              <i class="fas fa-lock" style="height: 24px; padding-top: 5px;"></i>
                          </span>
                      </div>
                  
                      {{form_widget(form.password)}}
                      <input type="text" style="display: none;" class="form-control form-control-md password-text" readonly>
                  </div>
                    
                    <input type="hidden" name="_csrf_token"
                          value="{{ csrf_token('authenticate') }}">

                    <div class="text-center pt-1 mb-4 pb-1">
                      <button class=" btn btn-primary mb-4 mt-4" type="submit" style="width: 100%;">
                         Se Connecter
                      </button><br/>
                      <a class="text-white" href="#">Mot de passe oublié</a>
                    </div>

                    <div class="d-flex flex-wrap align-items-center justify-content-center pb-4">
                        <p class="mb-0 me-2 compte">Vous n'avez pas encore un compte</p>
                        <a style="font-size: 13px;" href="{{path('app_register')}}" class="btn btn-outline-primary text-white mt-3"> Nouveau compte </a>
                      </div>
                  {{form_end(form)}}
                </div>
            </div>
        </div>
    </div>
</section>

{% endblock %}
